# Copy to frontend/.env.local and adjust.

# Base URL of the detection API. Leave empty to use the same origin, which is
# what the Vite dev proxy and the production nginx config both provide.
VITE_API_BASE_URL=

# Target the dev proxy forwards /api to (development only).
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
